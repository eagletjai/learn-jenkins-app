pipeline {
    agent any

    stages {

        stage('Playwright custom image') {
            steps {
                sh 'docker build -f playwright/Dockerfile -t my-playwright .'
            }
        }
    }
}